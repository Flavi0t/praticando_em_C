#include <stdio.h>

int main()
{
    int n, m, j;
    printf("Por favor, insira os tres numeros(inteiros) desejados:\n");
    scanf("%d%d%d", &n, &m, &j);
    printf("Soma dos numeros inseridos: %d\n", (n+m+j));
    return 0;
}